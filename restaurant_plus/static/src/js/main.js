openerp.restaurant_plus = function(instance) {
    "use strict";

    instance.point_of_sale = {};
    console.log("mak");
    console.log(instance);

    var module = instance.point_of_sale;

    // openerp_pos_db(instance,module);         // import db.js

    // openerp_pos_models(instance,module);     // import pos_models.js

    // openerp_pos_basewidget(instance,module); // import pos_basewidget.js

    // openerp_pos_keyboard(instance,module);   // import  pos_keyboard_widget.js

    // openerp_pos_screens(instance,module);    // import pos_screens.js

    // openerp_pos_devices(instance,module);    // import pos_devices.js
    
    // openerp_pos_widgets(instance,module);    // import pos_widgets.js

    // openerp_restaurant_multiprint(instance,module);

    // openerp_restaurant_splitbill(instance,module);

    // openerp_restaurant_printbill(instance,module);

    // restaurant_plus_db(instance,module);         // import db.js

    // restaurant_plus_models(instance,module);     // import pos_models.js

    // restaurant_plus_basewidget(instance,module); // import pos_basewidget.js

    // restaurant_plus_keyboard(instance,module);   // import  pos_keyboard_widget.js

    // restaurant_plus_screens(instance,module);    // import pos_screens.js

    // restaurant_plus_devices(instance,module);    // import pos_devices.js
    
    // restaurant_plus_widgets(instance,module);    // import pos_widgets.js

    // restaurant_plus_notes(instance,module);

    // restaurant_plus_splitbill(instance,module);

    // restaurant_plus_printbill(instance,module);

    // restaurant_plus_floors(instance,module);

    // restaurant_plus_multiprint(instance,module);

    // instance.web.client_actions.add('pos.ui', 'instance.point_of_sale.PosWidget');
};

    
